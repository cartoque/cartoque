#to-puppetize {
  border:1px solid #a3a; background-color:#fdf; padding:6px 10px; font-size:11px;
  h2 { color:#a3a; font-size:13px; padding:0; margin:0 0 5px 0; }
  .servers {
    color:#444; line-height:1.5em;
    a, a:hover { color:#444; }
    a:hover { text-decoration:underline; }
  }
}
