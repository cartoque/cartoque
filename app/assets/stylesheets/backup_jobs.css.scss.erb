#backup_jobs {
  .hierarchy { color:#222; }
  .exclusion_patterns { color:#777; }
}
#not-backuped {
  border:1px solid #e44; background-color:#fee; padding:6px 10px; font-size:11px;
  h2 { color:#e00; font-size:13px; padding:0; margin:0 0 5px 0; text-align:left; }
  .servers {
    color:#444; line-height:1.5em;
    a, a:hover { color:#444; }
    a:hover { text-decoration:underline; }
  }
}
