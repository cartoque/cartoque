@import 'mixins';
.graph-links { text-align:right; margin:5px 15px 5px 0; }
.graph-container { background-color:#fafafa; @include box-shadow(#ccc, 0, 0, 6px); margin:6px; }
.graph { margin:0 auto; }
svg a:hover { cursor:pointer; }
