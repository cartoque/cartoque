<%= simple_form_for(@service) do |f| %>
  <p class="actions">
    <%= f.button :submit %>
    ou <%= link_to "Retourner à la liste", services_path %>
  </p>

  <table class="show_item">
    <tbody>
      <%= tabular_errors @service %>
      <%= f.input :name, :required => true, :wrapper_class => "th" %>
    </tbody>
  </table>
<% end %>
