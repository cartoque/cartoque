<%= CSV.generate do |csv|
      @tomcats.sort_by do |tomcat|
        [ tomcat.server_name, tomcat.name ]
      end.each do |tomcat|
        csv << [tomcat.server_name, tomcat.name, tomcat.dns, tomcat.java_version]
      end
    end -%>
