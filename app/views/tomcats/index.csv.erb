<%= CSV.generate do |csv|
      @tomcats.sort_by do |tomcat|
        [ tomcat.server, tomcat.tomcat ]
      end.each do |tomcat|
        csv << [tomcat.server, tomcat.tomcat, tomcat.dns, tomcat.java_version]
      end
    end %>
