<%= action_links do %>
  <%= link_to "Nouveau thème", new_theme_path %>
<% end %>

<table class="list">
  <tr>
    <th>Nom</th>
    <th colspan="2" class="actions">Actions</th>
  </tr>
<% @themes.each do |theme| %>
  <tr>
    <td><%= theme.name %></td>
    <td><%= link_to 'Editer', edit_theme_path(theme) %></td>
    <td><%= link_to 'Supprimer', theme, :confirm => t(:text_are_you_sure), :method => :delete %></td>
  </tr>
<% end %>
</table>
