<%= action_links do %>
  <ul class="select-buttons">
    <%= content_tag :li, link_to("Comparaison Nagios", tool_path(:nagios_comparison)), :class => (params[:id] == "nagios_comparison" ? "current" : "") %>
    <%= content_tag :li, link_to("Comparaison VMWare", tool_path(:vmware_comparison)), :class => (params[:id] == "vmware_comparison" ? "current" : "") %>
  </ul>
<% end %>
